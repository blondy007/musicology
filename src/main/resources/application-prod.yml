spring:
  datasource:
    url: jdbc:mysql://${PROD_DB_USERNAME}:${PROD_DB_PASSWORD}@${PROD_DB_HOST}:${PROD_DB_PORT}/${PROD_DB_NAME}

  sql:
    init:
      mode: always # you won't do this in prod, I'm just doing this for demo purposes

